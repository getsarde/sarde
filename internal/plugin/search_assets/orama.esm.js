/* Orama placeholder — replace with upstream @orama/orama ESM bundle (https://oramasearch.com/). */
export const create = () => { throw new Error("orama placeholder — replace with real bundle"); };
export const insert = create;
export const insertMultiple = create;
export const search = create;
